<%= form_for(@user) do |f| %>
	<div class="field">
		<%= f.label :username %><br/>
		<%= f.text_field :username %><br/>
	</div>
	<div class="field">
		<%= f.label :password %><br/>
		<%= f.text_field :password %><br/>
	</div>
	<%= f.submit %>
<% end %>
